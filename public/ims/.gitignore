# IMS documents are served as static files from GitHub Pages at preqal.org/ims/
# Edit files on Mac (Preqal QMS folder), copy here, then commit + push.
# Supabase Storage upload script (scripts/upload-all-ims.cjs) is kept for
# future migration if private/auth-gated storage is needed.
